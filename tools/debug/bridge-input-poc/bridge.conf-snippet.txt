# Put these in the active b-bridge bridge.conf.
# They force the existing GTA-side DirectInput -> Windows-message translator
# to forward keyboard and mouse messages to the restored renderer channel.
client.DirectInput.forward.mousePolicy = 3
client.DirectInput.forward.keyboardPolicy = 3

# Leave this OFF for the first POC test.
# If bridge-input.log shows HANDSHAKE OK but NO keyboard/mouse messages,
# then try enabling it:
# client.hookMessagePump = True
